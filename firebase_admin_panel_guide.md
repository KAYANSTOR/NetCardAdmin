# دليل مطور لوحة التحكم (Admin Panel Documentation)

هذا الدليل يوضح هيكلية قواعد البيانات والمصادقة المطلوبة في **Firebase** لبرمجة لوحة التحكم الخاصة بإدارة تطبيق **NetCard Pro**. 

---

## 1. الإعدادات العامة (Global Settings)
مستند واحد للتحكم بحالة التطبيق لجميع المستخدمين، والتحكم بالفترة التجريبية الافتراضية.
- **المسار:** `app_settings/global_config`
- **الحقول الدقيقة:**
  - `is_app_active` (Boolean): إذا تم تعديلها إلى `false`، سيتم إغلاق التطبيق عند جميع المستخدمين فوراً وإظهار شاشة صيانة. (الافتراضي: `true`)
  - `maintenance_message` (String - اختياري): الرسالة التي ستظهر للمستخدمين عند الإغلاق العام للصيانة.
  - `default_trial_days` (Number): عدد الأيام الافتراضية للفترة التجريبية لأي مستخدم جديد يسجل من التطبيق (مثلاً `3` أو `7`).
  - `default_trial_warning` (String): نص رسالة التحذير التي تظهر للمستخدم التجريبي (مثلاً "أنت تستخدم النسخة التجريبية. تنتهي فترتك قريباً، الرجاء تصفية الحساب").

---

## 2. إدارة المستخدمين (أصحاب الشبكات)
**كيف يتم إنشاء الحسابات الآن؟**
لقد تم إعادة إتاحة "إنشاء حساب" للمستخدمين من داخل تطبيق الأندرويد.
عندما يقوم مستخدم جديد بإنشاء حساب من تطبيقه، سيقوم التطبيق برمجياً بـ:
1. إنشاء حسابه في Firebase Auth.
2. جلب عدد الأيام التجريبية `default_trial_days` من الإعدادات العامة.
3. إنشاء مستند له في مسار `users/{UID}` وحفظ تاريخ انتهاء فترته التجريبية.

**دورك كمبرمج للوحة التحكم:**
لوحة التحكم ستكون "لإدارة" هؤلاء المستخدمين (عرضهم، حظرهم، تمديد اشتراكهم، تحويلهم من تجريبي إلى رسمي).
عند عرض بيانات أي مستخدم، أو الرغبة في تعديل اشتراكه، ستتعامل مع المسار التالي:
- **المسار:** `users/{UID}`
- **الحقول التي سيعتمد عليها تطبيق الأندرويد للتحقق من صلاحية المستخدم:**
  - `is_active` (Boolean): حالة حساب العميل. إذا حولتها لـ `false` سيتم طرد العميل من التطبيق.
  - `is_trial` (Boolean): علامة توضح هل هذا المستخدم لا يزال في فترته التجريبية أم أنه مستخدم رسمي معتمد (تغييرها لـ `false` يعني أنه أصبح رسمياً).
  - `subscription_end_date` (Timestamp): تاريخ انتهاء اشتراك (أو تجربة) العميل. التطبيق يعتمد على هذا التاريخ لإظهار تحذيرات ثم توقيف التطبيق تماماً عند تجاوزه.
  - `warning_message` (String): نص رسالة التحذير المخصصة لهذا العميل والتي تظهر قبل انتهاء اشتراكه بـ 3 أيام. (يمكنك تعديلها من لوحة التحكم لتنبيهه بتصفية الحساب).
  - `commission_rate` (Number): نسبة أو قيمة عمولة الإدارة. تستخدمها أنت في لوحة التحكم لحساب المبلغ المطلوب من العميل.

---

## 3. حفظ بيانات الشبكة (Network Metadata)
يتم حفظ اسم الشبكة ورقم الهاتف الخاص بالعميل في هذا المسار الدقيق (يقوم التطبيق بكتابتها عند التسجيل، ويمكن للوحة التحكم قراءتها لعرض اسم ورقم العميل لك):
- **المسار:** `networks/{UID}/_metadata/info`
- **الحقول:**
  - `networkId` (String): وهو الـ `UID`.
  - `name` (String): اسم الشبكة.
  - `phoneNumber` (String): رقم هاتف صاحب الشبكة.
  - `description` (String): وصف.
  - `createdAt` (Number): تاريخ الإنشاء بصيغة Epoch Milliseconds.

---

## 4. إشعارات النظام (System Notifications)
لإرسال إشعارات وتنبيهات تظهر داخل شاشة الإشعارات في تطبيق الأندرويد للمستخدمين، يمكنك الكتابة في المسارات التالية:

**أ. إشعارات عامة (للجميع):**
- **المسار:** `app_settings/global_config/notifications/{notificationId}`
- **الحقول الدقيقة:**
  - `title` (String): عنوان الإشعار.
  - `message` (String): نص الإشعار.
  - `timestamp` (Number): وقت الإرسال بصيغة (Epoch Milliseconds).

**ب. إشعارات لمستخدم محدد:**
- **المسار:** `users/{UID}/notifications/{notificationId}`
- **الحقول الدقيقة:**
  - `title` (String): عنوان الإشعار.
  - `message` (String): نص الإشعار.
  - `timestamp` (Number): وقت الإرسال بصيغة (Epoch Milliseconds).
  - `is_read` (Boolean): حالة قراءة الإشعار (اجعلها `false`، والتطبيق سيحولها لـ `true` عند قراءتها).

---

## 5. سجلات المبيعات الواردة من التطبيق (Sales Sync)
تطبيق الأندرويد يقوم تلقائياً في الخلفية برفع جميع مبيعات العميل إلى المسار التالي لكي تقوم لوحة التحكم بقراءتها وحساب العمولات.
- **المسار:** `networks/{UID}/sales/{saleId}`
- **الحقول الدقيقة التي ستجدها مكتوبة بواسطة التطبيق:**
  - `saleId` (String): المعرف الفريد لعملية البيع.
  - `categoryId` (String): معرف فئة الكرت المباع.
  - `cardId` (String): معرف الكرت الذي تم بيعه.
  - `customerId` (String): رقم هاتف الزبون.
  - `posId` (String?): معرف نقطة البيع أو `null` إذا كان بيعاً مباشراً.
  - `faceValue` (Number): القيمة الاسمية للكرت (المبلغ الإجمالي).
  - `commission` (Number): عمولة نقطة البيع / الصراف.
  - `netAmount` (Number): الصافي (FaceValue - Commission).
  - `status` (String): `"COMPLETED"`، `"ROLLED_BACK"`، أو `"SMS_PENDING"`.
  - `createdAt` (Number): وقت البيع (Epoch Milliseconds).

*(ملاحظة للمبرمج: لحساب أرباح لوحة التحكم من هذا المستخدم، قم بجمع `faceValue` أو `netAmount` لجميع مبيعاته الناجحة `COMPLETED` في هذا المسار، ثم اضربها في `commission_rate` الخاصة به).*
